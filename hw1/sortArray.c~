#include <stdio.h>
#include <stdlib.h>

int main(int argc, char* argv[])
{
  
  FILE* fp = fopen(argv[1], "r");
  int length;
  fscanf(fp, "%d/n", &length);

  int array[length];
  int temp;

  for (int i = 0; i < length; i++)
  {
    fscanf(fp, "%d", &temp]);
 
  
  
    
    
  }
  


  fclose(fp);



  
}
